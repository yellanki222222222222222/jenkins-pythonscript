pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/your-username/jenkins-python-demo.git'
            }
        }

        stage('Run Python') {
            steps {
                bat 'python app.py'
            }
        }
    }
}
